import React, { Component } from 'react';
import LoginPanel from '../containers/login_panel';

export default class App extends Component {
  render() {
    return (
      <div>
        <LoginPanel />
      </div>
    );
  }
}
